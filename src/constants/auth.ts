export const AUTH_COOKIES = {
  ADMIN: "vivu-admin-auth",
  PUBLIC: "vivu-public-auth",
} as const;

export const STORAGE_KEYS = {
  ADMIN: "vivu-admin-auth",
  PUBLIC: "vivu-public-auth",
} as const;
